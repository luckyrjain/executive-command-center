---
id: PHASE-006-TEST-PLAN
title: Phase 6 Test Plan
status: Approved for Implementation
version: 0.10.1
owner: Lucky Jain
---

# Phase 6 Test Plan

**Task 1 status**: `tests/test_engineering_connectors_postgres.py` covers the sandbox adapter, connector account lifecycle, cursor durability across backfill/incremental sync, disconnect, and workspace isolation. Webhook dedupe, rate limits, access loss, deletion, rename, metric fixtures and ambiguous identities have no real provider or metric computation to test against yet -- each lands with the task that implements it (`docs/superpowers/plans/2026-07-27-phase-6-engineering-workspace.md`, Tasks 2-6).

**Task 2 status**: `tests/test_engineering_github_sync_postgres.py` closes the "rate limits" gap Task 1 left open -- `GitHubAdapter`'s bounded rate-limit retry is covered against a real (mocked-transport) GitHub response shape: a single rate-limited response that succeeds after the bounded wait, a rate limit that persists beyond the bound (`partial`, cursor preserved), and a rate limit that persists through the one retry as well (also `partial`, not an unhandled failure). Also covered: `authorize` (success/401/non-200/network-error/scope-checking), pagination via `Link`-header traversal, the incremental-cursor stop-early condition, the `_MAX_PAGES_PER_CALL` bound reporting `partial` rather than a silent `succeeded`, `refresh_permissions`, `disconnect`, `handle_webhook`'s parsing/upsert logic (not its receiving endpoint -- there isn't one yet), and end-to-end coverage through the real `/sync` endpoint including the concurrent-sync-in-progress guard (`uq_sync_runs_running_per_account`, migration `0046`). Access loss, deletion, rename and a real webhook-receiving endpoint remain untested because the code they'd test does not exist yet (Task 2's own disclosed deferrals -- see `github_adapter.py`'s module docstring); metric fixtures and ambiguous identities remain Task 5/6 scope.

**Task 3 status**: `tests/test_engineering_gitlab_sync_postgres.py` mirrors Task 2's own coverage shape against `GitLabAdapter`'s real API shapes, and (per its own review round) closes several edge cases beyond Task 2's own coverage: `authorize` (success/missing-scope/revoked-token/inactive-token/401/non-200/network-error on its first call, and -- a genuinely new failure mode GitHub's single-call `authorize` has no analogue for -- non-200/network-error on its *second* call, after the first has already validated the token; no fine-grained-PAT-style unverifiable case, since GitLab always returns real scopes, see `gitlab_adapter.py`'s module docstring), pagination via `Link`-header traversal (including a comma-containing `next` URL), the incremental-cursor stop-early condition, rate-limit retry succeeding/giving-up/still-limited-after-retry/giving-up-with-no-`Retry-After`-header/giving-up-with-a-malformed-`Retry-After`-value, the `_MAX_PAGES_PER_CALL` bound reporting `partial`, a generic non-200/429 failure raising, `refresh_permissions` (including its network-error fail-open branch), `handle_webhook`'s parsing/upsert logic proven against a real `Push Hook` payload (not just its two no-op branches), and `disconnect`'s real (not no-op) revocation attempt covering the success (204), already-revoked (404), connector's-own-scope-realistically-fails (403), and network-error paths. End-to-end coverage through the real `/sync` endpoint matches Task 2's own. Same deferrals as Task 2 (access loss, deletion, rename, real webhook-receiving endpoint, metric fixtures, ambiguous identities) for the identical reasons.

**Task 4 status**: `tests/test_engineering_jira_sync_postgres.py` mirrors Task 2/3's own coverage shape against `JiraAdapter`'s real API shapes: `authorize` (success -- asserting the honestly-empty `granted_scopes` since a Jira personal API token has no scope grant to report at all, malformed-credential rejection, 401/non-200/network-error), offset-based pagination (`startAt`/`maxResults`/`total`, not a `Link` header), the incremental-cursor stop-early condition, rate-limit retry succeeding/giving-up/still-limited-after-retry/giving-up-with-no-`Retry-After`-header/giving-up-with-a-malformed-`Retry-After`-value/a generic non-200/429 failure raising, the `_MAX_PAGES_PER_CALL` bound reporting `partial`, `refresh_permissions`, `disconnect`'s documented no-op, `handle_webhook`'s parsing/upsert logic proven against a real `jira:issue_updated` payload, and end-to-end coverage through the real `/sync` endpoint (backfill writing `engineering_work_items` rows, an incremental sync afterward only upserting genuinely newer rows, and a rate-limited sync reporting `partial` without moving the connector account to `error`). Same deferrals as Task 2/3 (access loss, deletion, rename, real webhook-receiving endpoint, metric fixtures, ambiguous identities) for the identical reasons; changes/reviews/deployments have no analogue for this provider at all, not merely a deferral.

**Task 5 status**: `tests/test_engineering_github_sync_postgres.py`'s new coverage: `_sync_changes` fan-out across two repositories, a closed-but-not-merged PR correctly excluded, the JSON per-repository cursor correctly persisting/resuming (repo1 already caught up, repo2 has a genuinely newer PR), the `_MAX_PAGES_PER_CALL` bound reporting `partial`, and rate-limit give-up reporting `partial`; `_sync_reviews`'s `requested_at` correctly captured from the earliest of multiple `review_requested` timeline events, a `PENDING` review with no `submitted_at` correctly excluded, the `merged_at`-based incremental cursor correctly stopping early, and a rate-limited timeline call correctly not advancing the cursor past its change (proven by re-checking `reviews` stays empty, not just the outcome status); end-to-end through the real `/sync` endpoint for `resource_type: "change"`. `tests/test_engineering_metrics_postgres.py` (new file): `_coverage_for`'s four states (no connected account, no source at all, one-of-two-stale, all-fresh), `work_ageing`/`blocked_work`'s population/bucketing/median-age correctness (including items missing `provider_created_at` counted in population but excluded from the age calculation), `review_latency`'s first-review-per-change median (a second, later review on the same change correctly excluded; a change missing `requested_at` or merged outside the 30-day window correctly excluded from `population`), all four deployment/incident-dependent metrics always reporting `insufficient_coverage`/`None` (as of Task 5; `time_to_restore` becomes real in Task 6, see below), `compute_and_store_metrics`'s insert-only behavior (calling it twice produces 14 rows, never overwrites the first 7), and end-to-end through the real `GET /engineering/metrics` endpoint including cross-workspace isolation.

**Task 6 status**: `tests/test_engineering_decisions_incidents_postgres.py` (new file, 47 tests after two review-fix rounds): `POST /incidents` (with/without `change_ids`, `CHANGE_NOT_FOUND` for a change outside the caller's workspace, a duplicate `change_id` in one payload correctly deduplicated rather than 500ing, invalid-severity/empty-title 422s, idempotency replay and conflict, missing-CSRF rejection), `POST /incidents/{id}/resolve` (success incrementing `version`, 404 when missing, 409 when already resolved, 422 when `resolved_at` precedes `detected_at`, idempotency replay and conflict, missing-CSRF rejection, cross-workspace 404, and a real `ThreadPoolExecutor` concurrency test proving two concurrent resolve calls with different Idempotency-Keys never both succeed -- one gets `409 INCIDENT_ALREADY_RESOLVED`, closing the lost-update race the review found), `GET /incidents` with and without the `status` filter plus cross-workspace isolation, the identical shape for `POST /decisions`/`POST /decisions/{id}/decide` (including the not-provided-rationale-keeps-the-existing-value branch, and decide's own cross-workspace 404) and `GET /decisions` with cross-workspace isolation, and every migration CHECK constraint (`ck_incidents_severity`/`ck_incidents_status`/`ck_incidents_resolved_at_matches_status`/`ck_engineering_decisions_status`/`ck_engineering_decisions_decided_at_matches_status`, including the `superseded` branch's positive and negative cases) exercised directly at the SQL layer in both directions. `tests/test_engineering_metrics_postgres.py` gains `_compute_time_to_restore` coverage: median duration across two resolved incidents in the 30-day window, an open incident and one resolved outside the window both correctly excluded from `population`, an exact-30-day-boundary inclusion test called directly against the function with an explicit `now` (avoiding the timing-skew risk Task 5's `_bucket_ages_days` boundary test already had to work around), coverage unconditionally `complete` (including with zero incidents), and cross-workspace isolation.

**Task 7 status**: `tests/test_engineering_write_actions_postgres.py` (39 tests after three review-fix rounds, up from 17): the shared production registry resolves all three new adapters, each declaring `high_impact_categories == {"public"}` and `reversible is True`; each adapter's `execute()` against a real Postgres-backed `connector_accounts`/`repositories`/`engineering_work_items` fixture with a mocked HTTP transport -- success (output never carries `body`), the connector-account-not-found/wrong-workspace/wrong-provider/inactive-connector rejections, and (GitHub/Jira only) the repository-not-synced/work-item-not-synced containment rejection; retry-safety classification (a pre-send connection failure and an HTTP 429 both raise `TransientAdapterError`, a read timeout and a 4xx/5xx response both raise a plain non-retried exception); `simulate()` proven to make no network call at all (a mock transport that raises if invoked); and one true end-to-end test through the real `ecc.domains.automation.worker` dispatch path for `github.add_issue_comment` -- `high_impact_categories={"public"}` pauses the run on `waiting_approval` before any HTTP call is made, and only dispatches (making the mocked HTTP call exactly once) after a real `decide_approval("approved")` call, mirroring `tests/test_automation_adapters_postgres.py`'s own `fake.external_action`/`local.send_test_notification` precedent. `gitlab.add_note` and `jira.add_comment` now carry the identical cross-workspace/wrong-provider/disconnected-account/retry-safety/simulate coverage `github.add_issue_comment` already had (the initial version under-tested them despite sharing the same code paths, and round 2 further fixed the github/jira wrong-provider tests, which had been vacuous -- they passed a never-inserted target id, so the containment check rejected the call before the provider check under test ever ran); `jira.add_comment`'s success test additionally asserts the real Basic-auth header, the exact ADF request body, and that `output.source_url` is built from the work item's real stored `source_url` rather than its numeric `external_id` (`assert "10001" not in output.source_url`), directly proving the review's `source_url` bug fix; a `gitlab.add_note` test proves the disclosed no-containment-check asymmetry is real, not merely incidental, by succeeding against a never-synced `project_path`/`issue_iid`; 5 parametrized tests prove `GitLabAddNoteInput`'s `project_path` validator rejects every `"."`/`".."`/empty path-segment value at input validation, before any network call -- closing a real path-escape bug the round-2 security review found (`quote(..., safe="")` never encodes `.`, so an unencoded `..` dot-segment could redirect the POST outside the intended `/projects/*` subtree); and a new `test_load_credential_rejects_connector_account_in_different_workspace` isolates `_load_credential`'s own workspace-scoping check directly (via a module-qualified import, this file's own new precedent for exercising a private helper the way `test_engineering_jira_sync_postgres.py` already does for `_parse_credential`), since round 3 found the github/jira cross-workspace adapter-level tests cannot be fixed the way the wrong-provider tests were -- `repositories`/`engineering_work_items`'s own compound FK on `(workspace_id, connector_account_id)` makes constructing a containment row for a genuinely cross-workspace account architecturally impossible (confirmed empirically via `IntegrityError`), so those two tests remain adapter-level, non-isolating proofs by necessity, with docstrings added explaining why and pointing at the new isolating test.

**Task 8 status**: `tests/test_engineering_query_endpoints_postgres.py` (11 cases, new file) covers the two disclosed additive endpoints this task adds (`GET /engineering/repositories`, `GET /engineering/work-items`): workspace-scoped listing ordered by `name`/`title`, the optional `connector_account_id` filter on both, the optional `status` filter on work items (a plain string match, not a closed enum), cross-workspace isolation on both, an empty list (not an error) when nothing has synced, and `reporter_external_id`/`assignee_external_id` round-tripping exactly as stored -- the field the frontend's conflicting-identities disclosure depends on being returned unmodified. Frontend coverage (`frontend/src/features/engineering/*.test.tsx`, 76 cases across 9 files after three review-fix rounds, up from 55): the `EngineeringWorkspace` shell (tab defaults, switching, roving-tabindex keyboard nav, all 8 required surfaces present), `ConnectorHealthPanel` (18 cases covering every UX-STATES.md-required state -- first sync, backfill, partial permissions, stale connector, rate limited, disconnected, provider unavailable -- plus create/sync/disable actions, a `partial`/`failed` sync-run status pair (added by round 1, since neither had ever been tested despite `partial` being normal/expected behavior per every provider adapter's own docstring), a failed-disconnect test (added by round 2, closing the last untested mutation-`isError` branch in this file), and every reachable error code mapped to a readable sentence), `RepositoriesPanel` (permission/freshness badges, empty state, load failure), `IncidentsPanel`/`DecisionsPanel` (list/filter/create/resolve-or-decide/error mapping, each now also covering a load failure and -- added by round 2 -- a failed-create test closing the one mutation-`isError` branch round 1 had missed), `DeliveryPanel`/`ReliabilityPanel` (definition/window/coverage/evidence always shown together per design doc Decision 3, `insufficient_coverage` never showing a misleading number, no person-scoped text anywhere, a load failure, the real seconds-to-days unit conversion for `time_to_restore`/`review_latency` and the `work_ageing`-is-a-duration-not-a-count distinction from `blocked_work` (added by round 1), and -- added by round 3 -- the `change_failure_rate` percentage conversion, the one `MetricCard` formatting branch round 2 had left untested), `CoveragePanel` (per-connector rollup, the degraded/all-healthy banner, the conflicting-identities disclosure proven to show a sample list rather than an interactive merge control, and a load failure), and `EngineeringOverview` (summary counts including that a disconnected connector counts as neither connected nor needing attention, quick-jump navigation callbacks, a load failure, and -- added by round 2, closing a real bug round 2's correctness review found -- a test proving the "Headline metrics" list applies the same seconds-to-days conversion as `MetricCard`, since that list renders metric values independently via its own `formatValue` reuse rather than through `MetricCard` itself). Browser acceptance (`frontend/e2e/scenarios/engineering-connector-states.mjs`, `engineering-lifecycle.mjs`, both new, run via `frontend/e2e/run.mjs` against a `makeEngineeringApi` in-memory fixture mirroring `makeAutomationApi`'s own shape) exercises every required degraded state end to end with an `@axe-core/playwright` accessibility check after each tab switch -- the browser-acceptance run itself caught and closed a real component bug (`ConnectorHealthPanel`'s "never synced" state was keyed on `connector.status === 'pending'` rather than `syncRuns.length === 0`, so it stayed shown even once a first backfill was already running) before this task's own PR was opened, exactly the kind of defect a purely-mocked unit test could not have caught. `engineering-lifecycle.mjs`'s own metric fixture rows were corrected during the review-fix round to use realistic raw-seconds `value`s (matching what `metrics.py` actually computes) rather than pre-converted days figures, which had been silently masking the `MetricCard.tsx` unit bug above.

**Final whole-phase review status**: five persona reviews (correctness, security, API-contract, test-coverage, governance) read across all 8 merged tasks looking for cross-task issues, closed in `IMPLEMENTATION-STATUS.md`'s "Final whole-phase review" section. New/corrected tests from that round: `tests/test_engineering_metrics_postgres.py` gains `test_insufficient_coverage_nulls_the_value_even_when_previously_synced_rows_exist` and `test_review_latency_insufficient_coverage_nulls_the_value` (proving `value`/`numerator`/`denominator`/`details` are null once coverage regresses to `insufficient_coverage`, even with a real, nonzero `population` from previously-synced rows), with two pre-existing tests (`test_work_ageing_counts_items_missing_created_at_separately`, `test_details_json_round_trips_through_the_database`) corrected to insert a fresh sync cursor -- both had been passing only by the accident of never doing so, which the fix above would otherwise have broken; `tests/test_engineering_query_endpoints_postgres.py` gains three tests proving `disable_connector_endpoint` now cascades `freshness_state='disconnected'` to `repositories`/`engineering_work_items`, scoped correctly to the disabled connector account and not a sibling one, plus a fourth test (`test_disable_marks_changes_and_reviews_disconnected`, added by a round-2 correctness re-review that found the first fix pass had left `changes`/`reviews` off the cascade) proving those two tables -- which have no `GET` endpoint yet, so this checks the column directly -- are covered too; `tests/test_production_security.py` gains `test_engineering_metrics_get_is_mutation_rate_limited`, proving `GET /engineering/metrics` is now rate-limited like a real mutation while an unrelated GET stays unaffected; `frontend/src/features/engineering/IncidentsPanel.test.tsx`/`DecisionsPanel.test.tsx` each gain a test proving a stale CSRF token now maps to the correct "reload the page" message instead of falling through to a misleading "not permitted" 403, bringing the frontend suite from 76 to 78 cases. the added `workspace_id` join guard in `_list_changes_needing_reviews` is covered by this file's existing sync/review tests continuing to pass unchanged (same-behavior-for-valid-input hardening, not a new branch). `github_adapter.py`'s new `_safe_repo_path_segment` defense-in-depth helper had zero direct test coverage in the initial fix pass -- a round-2 test-coverage re-review found this gap and closed it with 9 new parametrized unit tests in `tests/test_engineering_github_sync_postgres.py` (6 rejection cases mirroring `GitLabAddNoteInput._reject_dot_segments`'s own precedent, 3 passthrough cases proving a real repo name containing a literal `.` inside a segment, e.g. `acme/repo.name`, is not mistaken for a dot-segment), bringing that file from 39 to 48 cases.

Test sandbox adapters, backfill/incremental sync, cursor durability, webhook dedupe, rate limits, access loss, deletion, rename, disconnect and rebuild. Validate metric fixtures and definitions against hand-calculated results. Verify partial coverage and ambiguous identities.

Security covers token redaction, least scopes, webhook signatures, malicious payloads, isolation and approved writes. Ethics checks prohibit person scores/leaderboards. Browser acceptance connects a sandbox, observes sync/coverage, traces a risk to evidence and handles degraded states.
